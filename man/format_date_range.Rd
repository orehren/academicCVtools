% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{format_date_range}
\alias{format_date_range}
\title{Combine start and end date columns into a formatted date range string}
\usage{
format_date_range(
  data,
  start_col,
  end_col,
  output_col = "date_range",
  output_format = "\%m/\%Y",
  sep = " - ",
  ongoing_label = "Present"
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{start_col}{The name of the column containing the start dates (character).}

\item{end_col}{The name of the column containing the end dates (character).}

\item{output_col}{The name for the new column that will contain the
formatted date range string (character). Defaults to \code{"date_range"}.}

\item{output_format}{A string specifying the desired output format for the
dates, using standard R \code{strftime} specifiers (e.g., \code{"\%Y"}, \code{"\%m/\%Y"},
\code{"\%d/\%m/\%Y"}). Defaults to \code{"\%m/\%Y"}.}

\item{sep}{The separator string to use between the formatted start and
end dates. Defaults to \code{" - "}.}

\item{ongoing_label}{The string to use for the end date if it is \code{NA} or
interpreted as ongoing. Defaults to \code{"Present"}.}
}
\value{
The input data frame with an added column containing the
formatted date range strings.
}
\description{
This function takes a data frame and the names of two columns containing
start and end dates (expected to be R Date objects or coercible to Date).
It creates a new column with a formatted string representing the date range
(e.g., "YYYY - YYYY", "MM/YYYY - Present").
}
\examples{
\dontrun{
df <- dplyr::tibble(
  start_date = as.Date(c("2020-01-01", "2021-05-01", NA, "2022-03-01")),
  completion_date = as.Date(c("2020-12-31", NA, "2021-08-15", "2022-03-01"))
)

# Basic usage
format_date_range(df, "start_date", "completion_date")

# Different output format and column name
format_date_range(df, "start_date", "completion_date",
                  output_col = "Period", output_format = "\%Y")

# Different separator and ongoing label
format_date_range(df, "start_date", "completion_date",
                  sep = " to ", ongoing_label = "Current")

# Handle cases where start_date might be a string coercible to Date
df_string <- dplyr::tibble(
  begin = c("2020-01-01", "2021/05/01"),
  end = c("2020-12-31", NA)
)
format_date_range(df_string, "begin", "end")
}
}
